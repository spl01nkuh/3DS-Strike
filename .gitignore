build/
build_wsl/
*.obj
*.o
*.d
# Anchored to the repo root: an unanchored "PSP/" also matches src/psp/ on
# case-insensitive filesystems, which silently kept 14 core port sources
# (adx.c, afs.c, fl.c, PPGFile.c, ...) out of the repo.
/PSP/
3s-psp.zip
3s-psp-experimental.zip
dist/
*.newport
build1.log
